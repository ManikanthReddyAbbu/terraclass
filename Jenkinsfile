pipeline { 
    agent any 
    stages {
        stage('Build') { 
            steps { 
                sh 'date' 
            }
        }
        stage('Test'){
            steps {
                sh 'date'
                
            }
        }
        stage('Deploy') {
            steps {
                sh 'date'
            }
        }
    }
}
